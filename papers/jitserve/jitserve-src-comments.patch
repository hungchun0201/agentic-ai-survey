diff --git a/jitserve/request_analyzer/graph_context.py b/jitserve/request_analyzer/graph_context.py
index 21a2fac..fba158f 100644
--- a/jitserve/request_analyzer/graph_context.py
+++ b/jitserve/request_analyzer/graph_context.py
@@ -1,3 +1,5 @@
+"""Pattern-graph matcher for compound requests. Paper §4.2 "How compound requests are handled"."""
+
 from __future__ import annotations
 
 from typing import Dict, List, Optional, Set, Tuple
@@ -83,13 +85,33 @@ class GraphMatchContext:
                               is_deepresearch: bool = False,
                               stage_id: int = 0,
                               accumulate_stage_ratio: float = 0.0) -> float:
+        # KEY DESIGN: this is the entry point GMAX calls every tick to
+        # learn what FRACTION of the program-level deadline the current
+        # stage is allowed to consume. Per-call length prediction alone
+        # does not answer that question; only the aggregate of remaining
+        # stages does. Everything below trades prediction quality
+        # against per-tick cost.
+        # 關鍵設計:此函式是 GMAX 每 tick 呼叫的進入點,用於得知
+        # 當前階段被允許消耗程式級 deadline 的「比例」。Per-call 長度
+        # 預測單獨無法回答此問題;唯有對「所有剩餘階段累計成本」做
+        # 估計才行。下方各分支即在「預測品質」與「每 tick 成本」之
+        # 間取捨。
         if self.use_total_deadline:
-            return 1.0
+            return 1.0   # degenerate: whole program budget for this stage
+                         # 退化情況:整個程式預算都歸此階段使用
 
         default_ratio = self._default_ratio(is_deepresearch, stage_id)
         if self.graph_matching_mode == "none":
+            # No historical match -- fall back to a hard-coded prior
+            # (Default_DeepResearch / Default_ToT pattern). Cheapest mode,
+            # blind to which workflow this actually is.
+            # 無歷史匹配——退回硬編碼的先驗分佈
+            # (Default_DeepResearch / Default_ToT 樣式)。最便宜的模式,
+            # 對實際工作流類型無感。
             return default_ratio
         if self.graph_matching_mode == "precise":
+            # An oracle ratio was passed in (e.g., for the JITServe* upper-bound
+            # variant in §6.2). Used only in evaluation, not in deployment.
             if is_deepresearch and accumulate_stage_ratio > 0.0:
                 logger.info(
                     f"Using precise accumulate_stage_ratio: {accumulate_stage_ratio}"
@@ -97,10 +119,22 @@ class GraphMatchContext:
                 return accumulate_stage_ratio
             return default_ratio
         if self.graph_matching_mode == "static":
+            # KEY DESIGN: match ONCE at admission and reuse the prediction
+            # across all stages. Cheap (one LSH lookup per program) but
+            # cannot react if the actual graph diverges from the match.
+            # 關鍵設計:於 admission 時做「一次」匹配,所有階段重用此
+            # 預測。便宜(每個 program 只一次 LSH 查找),但若實際 graph
+            # 偏離匹配結果,無法即時修正。
             return self._static_ratio(num_stages, requests_per_stage, prompt,
                                       collection_id, is_deepresearch, stage_id,
                                       default_ratio)
         if self.graph_matching_mode == "online":
+            # KEY DESIGN: re-match at EVERY stage as the partial graph grows.
+            # The match becomes more accurate as more of the graph is observed,
+            # at the cost of one LSH lookup per tick per active compound request.
+            # 關鍵設計:於「每一階段」隨部分 graph 增長重新匹配。隨更多
+            # graph 被觀察,匹配越精確;代價是每個進行中的複合請求每
+            # tick 一次 LSH 查找。
             return self._online_ratio(num_stages, requests_per_stage, prompt,
                                       collection_id, is_deepresearch, stage_id)
 
@@ -214,6 +248,12 @@ class GraphMatchContext:
             collection_id]
         unfinished_graph = deepresearch_structure.convert_to_unfinished_graph(
             self._encode_prompt_len(prompt), None)
+        # LSH retrieval against the historical (graph_pattern, runtime) database.
+        # Returns the closest finished workflow whose execution graph matches
+        # the prefix observed so far for this in-progress compound request.
+        # 對歷史(graph_pattern, runtime)資料庫做 LSH 檢索。回傳已完成
+        # 工作流中,其 execution graph 與當前進行中複合請求至此觀察到的
+        # 前綴最接近者。
         best_match = self.dynamic_clustering.find_best_match(unfinished_graph)
         logger.info(
             f"DeepResearch collection_id: {collection_id}, stage_id: {stage_id}, best_match: {best_match}"
@@ -222,6 +262,16 @@ class GraphMatchContext:
             ratio_1 = sum(best_match.times[:stage_id]) / sum(best_match.times)
             ratio_2 = sum(Default_DeepResearch_Requests_Pattern[:stage_id]) / sum(
                 Default_DeepResearch_Requests_Pattern)
+            # KEY DESIGN: take the LARGER of the historical ratio and the
+            # default prior -- same asymmetry principle as L_up. If the
+            # historical match underestimates the current stage's cost,
+            # the fallback prior protects against an irrecoverable miss;
+            # if the prior under-estimates instead, the historical match
+            # corrects upward. Always biased toward over-reservation.
+            # 關鍵設計:取「歷史比例」與「預設先驗」的較大值——與 L_up
+            # 同樣的不對稱原則。若歷史匹配低估當前階段成本,預設先驗
+            # 可防止不可恢復的 miss;若先驗低估則由歷史匹配向上修正。
+            # 永遠偏向過度預留。
             return max(ratio_1, ratio_2)
         return sum(Default_DeepResearch_Requests_Pattern[:stage_id]) / sum(
             Default_DeepResearch_Requests_Pattern)
diff --git a/jitserve/request_analyzer/prediction.py b/jitserve/request_analyzer/prediction.py
index 6b14f38..0c3a91c 100644
--- a/jitserve/request_analyzer/prediction.py
+++ b/jitserve/request_analyzer/prediction.py
@@ -1,3 +1,5 @@
+"""QRF backend that produces L_up_hat. Paper §4.2 "How L_up is estimated"."""
+
 import asyncio
 import socket
 import numpy as np
@@ -15,16 +17,39 @@ def load_model(model_path: str, tokenizer_path: str) -> Tuple:
         raise Exception("Model or tokenizer not found")
     return model, tokenizer
 
-# Perform prediction on the input using the loaded model and vectorizer
 def predict(vectorizer, model, input):
     X_test_vec = vectorizer.transform(input)
+    # KEY DESIGN: ask for the P95 quantile, not the mean. The two error
+    # directions are asymmetric -- underestimating L_up causes insufficient
+    # bandwidth reservation and an irrecoverable SLO miss; overestimating
+    # only produces a brief over-reservation that the scheduler reclaims
+    # on the next tick. A high quantile is therefore strictly preferable
+    # even when the mean would be more "accurate" on average.
+    # 關鍵設計:要求 P95 分位數而非平均值。兩個誤差方向不對稱——
+    # 低估 L_up 造成預留頻寬不足、不可恢復的 SLO 違反;高估僅造成
+    # 短暫的過度預留,排程器於下一個 tick 即可回收。因此即便平均值
+    # 在「平均意義」上較精確,高分位仍嚴格優於平均。
     y_pred_i: np.array = model.predict(X_test_vec.toarray(), quantiles=[0.95])
     return [int(element) for element in y_pred_i]
 
-# Asynchronous function to handle prediction and return result
+
 async def async_predict(vectorizer, model, input, request_info: RequestInfo) -> None:
+    # KEY DESIGN: QRF runs in single-digit ms but is still wrapped in an
+    # asyncio executor so it can never block the scheduler tick. Compare
+    # with using a Llama-3-class LLM as a length oracle, which would
+    # itself be slower than just decoding the response -- the cost of
+    # being right would equal the cost of being wrong. QRF is cheap
+    # enough that the wrapping is precaution, not necessity.
+    # 關鍵設計:QRF 執行時間在個位數毫秒,仍以 asyncio executor 包裝,
+    # 確保它不會阻塞排程器 tick。對比 Llama-3 等級 LLM 作為長度神諭,
+    # 其本身比直接 decode 回覆還慢——「預測對」與「預測錯」成本相等。
+    # QRF 已足夠便宜,asyncio 包裝在此屬預防性質,非必要。
     loop = asyncio.get_event_loop()
     result = await loop.run_in_executor(None, predict, vectorizer, model, input)
+    # The scheduler reads L_up_hat from RequestInfo.output_len on the next
+    # tick. This is the only place L_up enters shared state.
+    # 排程器於下一個 tick 由 RequestInfo.output_len 讀取 L_up_hat。
+    # 這是 L_up 進入共享狀態的唯一位置。
     request_info.output_len = result[0]
 
 
diff --git a/jitserve/scheduler/policy.py b/jitserve/scheduler/policy.py
index 1a569d9..843c307 100644
--- a/jitserve/scheduler/policy.py
+++ b/jitserve/scheduler/policy.py
@@ -1,3 +1,5 @@
+"""GMAX scheduler (SLOPolicy) and the §3.3 baselines. Paper §4.2 b_min, §6.3 marginal goodput."""
+
 import time
 from abc import ABC, abstractmethod
 from collections import deque
@@ -249,8 +251,32 @@ class SLOPolicy(BasePolicy):
 
     def _compute_remain_time(self, seq_group: SequenceGroup, remain_len: int,
                              unit_time: float) -> float:
+        # KEY DESIGN: this is where the per-class delta_t of paper §4.2 enters.
+        # The same formula b_min = (L_up - L_done) / delta_t is used everywhere;
+        # only delta_t differs.
+        # 關鍵設計:此處是論文 §4.2 各 SLO 類別 delta_t 進入計算之處。
+        # 同一公式 b_min = (L_up - L_done) / delta_t 適用所有類別;
+        # 唯一差別是 delta_t 的構造方式。
         if seq_group.request_type == RequestType.COLLECTIVE:
+            # Compound (multi-call) request: delta_t for the CURRENT call is
+            # the program-level deadline reduced by the predicted aggregate
+            # cost of all DOWNSTREAM stages. allocation_control() walks the
+            # collection's siblings and sums their predicted remaining time;
+            # what is left is the budget for this stage.
+            # 複合(多呼叫)請求:當前呼叫的 delta_t 為程式級 deadline
+            # 扣除所有「下游階段」預測累積成本後的餘量。
+            # allocation_control() 走訪同 collection 的 sibling 並加總
+            # 其預測剩餘時間;扣除後即為當前階段的預算。
             return max(self.allocation_control(seq_group), 1e-6)
+        # Single-call (latency-sensitive or deadline-sensitive): delta_t is
+        # local. unit_time is the average scheduler-tick interval, so this
+        # is "predicted wall-clock to finish remain_len tokens at the
+        # current scheduling rate" -- a finish-time estimate, equivalent
+        # to b_min's denominator after a change of variables.
+        # 單呼叫(延遲敏感或截止時間敏感):delta_t 為局部值。
+        # unit_time 為排程 tick 平均間隔,因此這個值代表「以當前排程
+        # 速率完成 remain_len 個 token 所需的預測 wall-clock」——
+        # 一個完成時間估計,經變數代換後等價於 b_min 的分母。
         return max(unit_time * remain_len, 1e-6)
 
     def _estimate_latency_reward(self, seq_group: SequenceGroup, input_len: int,
@@ -286,8 +312,21 @@ class SLOPolicy(BasePolicy):
     def reward_estimation(self, seq_group: SequenceGroup) -> Tuple[float, float]:
         cur_time = time.time()
         input_len = seq_group.seqs[0].get_prompt_len()
-        decode_len = seq_group.seqs[0].get_decode_len()
-        predict_output_len = seq_group.request_info.output_len
+        decode_len = seq_group.seqs[0].get_decode_len()                  # = L_done(已產生 token 數)
+        predict_output_len = seq_group.request_info.output_len           # = L_up_hat,來自 QRF
+        # KEY DESIGN: this is the numerator of paper equation (1):
+        #     b_min = (L_up_hat - L_done) / delta_t
+        # remain_len shrinks every tick as decode_len grows; if the actual
+        # response approaches L_up_hat, remain_len -> 0 and the request
+        # naturally drops off the priority queue. If the actual response
+        # OVERSHOOTS L_up_hat, remain_len goes negative -- handled by the
+        # preemption rule (paper §7.2) which fires when the bound was wrong.
+        # 關鍵設計:這是論文公式 (1) 的分子:
+        #     b_min = (L_up_hat - L_done) / delta_t
+        # remain_len 隨 decode_len 每 tick 增長而縮小;若實際回覆接近
+        # L_up_hat,remain_len → 0,請求自然從優先佇列退出。若實際
+        # 回覆「超出」L_up_hat,remain_len 轉負——由 §7.2 搶占規則
+        # 處理(亦即「上界錯了」的情境)。
         remain_len = predict_output_len - decode_len
 
         unit_time = self._get_interval_time()
@@ -320,6 +359,18 @@ class SLOPolicy(BasePolicy):
         
         if seq_group.request_type == RequestType.LATENCY or seq_group.request_type == RequestType.THROUGHPUT:
             reward, remain_time = self.reward_estimation(seq_group)
+            # KEY DESIGN: density = reward / remain_time = MARGINAL GOODPUT
+            # PER UNIT TIME. This is the operational form of b_min from
+            # paper §4.2 -- a request whose remain_time is shrinking
+            # (whose b_min is therefore rising) gets a proportionally
+            # larger priority next tick. Negated because Python's heap
+            # is min-heap; the request with the LARGEST density floats
+            # to the front of the GMAX priority queue.
+            # 關鍵設計:density = reward / remain_time = 「每單位時間的
+            # 邊際 goodput」。這是論文 §4.2 中 b_min 的運算形式——
+            # remain_time 縮減中(亦即 b_min 上升中)的請求,下一個
+            # tick 取得相應較大的優先級。取負是因 Python heap 為
+            # min-heap;density 最大者浮至 GMAX 優先佇列前端。
             density = reward / remain_time
             slo_priority = (-density, remain_time)
             self.seq_group_slo_dict[seq_group.request_id] = slo_priority
diff --git a/jitserve/slo_tracker/slo_tracker.py b/jitserve/slo_tracker/slo_tracker.py
index e852825..6262f4f 100644
--- a/jitserve/slo_tracker/slo_tracker.py
+++ b/jitserve/slo_tracker/slo_tracker.py
@@ -1,3 +1,5 @@
+"""Per-tick service-gain tracker. Feeds GMAX reward; paper §6.3 marginal goodput per SLO class."""
+
 import time
 
 from typing import Iterable, Optional, TYPE_CHECKING
@@ -85,7 +87,23 @@ class SLOTracker:
             if desire_decode_len <= 0:
                 service += delta_output
             else:
+                # decode_ratio < 1 means the request is BEHIND its rate target.
+                # decode_ratio < 1 表示請求落後於其速率目標。
                 decode_ratio = self._clamp_ratio(output_len, desire_decode_len)
+                # KEY DESIGN: raise to penalty_factor. A small lag in decode
+                # rate produces a small reduction in service gain, but a
+                # large lag produces a much larger reduction -- next-tick
+                # density (= reward / remain_time) drops disproportionately,
+                # so the request's GMAX priority falls. This is what makes
+                # the system shed irrecoverable requests gracefully under
+                # load (paper §10.4, Fig 15) instead of letting one
+                # struggling request pull everyone else down.
+                # 關鍵設計:取 penalty_factor 次方。decode 速率小幅落後
+                # 僅造成 service gain 小幅減少,但大幅落後則造成不成比例
+                # 的大幅減少——下一 tick 的 density(= reward / remain_time)
+                # 按比例下降,該請求的 GMAX 優先級隨之下降。這正是系統
+                # 在負載上升時得以「優雅地捨棄不可挽回的請求」(論文
+                # §10.4 圖 15)而不被個別掙扎請求拖垮整體的機制。
                 service += decode_ratio**self.penalty_factor * delta_output * RequestPhaseWeight.DECODE.value
 
         return service
@@ -109,6 +127,20 @@ class SLOTracker:
 
     def _desired_decode_len(self, seq_group: "SequenceGroup",
                             cur_time: float) -> float:
+        # KEY DESIGN: this IS the latency-class b_min, expressed inversely.
+        # Instead of "tokens per second I must produce", we ask "how many
+        # tokens should I have produced by now to honour the TBT_constraint
+        # rate target?" The denominator is TBT_constraint = the contracted
+        # time per token; the numerator is elapsed time since first token.
+        # _latency_service_gain compares this with the actual output_len
+        # below and penalises any shortfall, which is the per-tick
+        # mechanism that escalates a falling-behind request in priority.
+        # 關鍵設計:此函式即延遲類 b_min 的反向表達。不問「我每秒需
+        # 產生多少 token」,而是問「為了遵守 TBT_constraint 速率目標,
+        # 我至此刻應已產生多少 token?」分母 = TBT_constraint,即合約
+        # 定義的每 token 時間;分子 = 自首 token 起的經過時間。
+        # _latency_service_gain 將此值與實際 output_len 比較,對不足者
+        # 施以懲罰——此即落後請求被提升優先級的每 tick 機制。
         metrics = seq_group.concord_metrics
         if metrics.TTFT is None:
             return 0.0
