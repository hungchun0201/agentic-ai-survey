{
  "slug": "dlrm-l2pin",
  "domains": [
    "inference-modeling"
  ],
  "title": "Pushing the Performance Envelope of DNN-based Recommendation Systems Inference on GPUs",
  "authors": "Rishabh Jain; Vivek M. Bhasi; Adwait Jog; Anand Sivasubramaniam; Mahmut T. Kandemir; Chita R. Das",
  "year": "2024-10",
  "venue": "MICRO",
  "venue_full": "57th IEEE/ACM International Symposium on Microarchitecture (MICRO 2024)",
  "arxiv": "2410.22249",
  "url": "https://arxiv.org/abs/2410.22249",
  "affiliations": "The Pennsylvania State University, University of Virginia",
  "research_focus": "GPU memory tiering, Hardware co-design, Performance modeling, Roofline analysis",
  "problem_statement": {
    "en": "In DNN-based recommendation (DLRM) GPU inference, the embedding stage is the primary bottleneck, causing up to a 3.2x embedding-only slowdown because gather operations over large embedding tables incur frequent, irregular memory accesses with low SM occupancy and long-latency global-memory load stalls. The paper asks how to keep frequently reused (hot) embedding rows close to the cores instead of repeatedly fetching them from HBM across successive kernel invocations.",
    "zh": "在基於 DNN 的推薦模型（DLRM）GPU 推理中，embedding 階段是主要瓶頸，造成最高達 3.2 倍的 embedding-only 變慢：對大型 embedding table 做 gather 會產生頻繁且不規則的記憶體存取，導致 SM 佔用率偏低與長延遲的 global-memory load stall。論文探討如何把頻繁重用的熱門 embedding row 留在靠近運算核心的位置，而不是在連續的 kernel 呼叫之間反覆從 HBM 重新讀取。"
  },
  "key_innovation": {
    "en": "Three composable, plug-and-play kernel-level techniques layered on the PyTorch EmbeddingBag CUDA kernel: (1) OptMT — direct compiler optimizations (e.g. capping register usage via -maxrregcount to lift warp-level parallelism/occupancy); (2) RPF — register-resident software prefetching that issues loads early to overlap long-latency stalls; and (3) L2P (L2 pinning) — the cache-reuse contribution: a dedicated prefetch_pin_L2 kernel issues inline PTX 'prefetch.global.L2::evict_last' on the hottest embedding rows so those rows are pinned with highest eviction priority in the GPU L2 cache and stay resident ACROSS kernel launches, turning otherwise cold cross-kernel HBM re-reads into L2 hits.",
    "zh": "在 PyTorch EmbeddingBag 的 CUDA kernel 上疊加三項可組合、即插即用的 kernel 層技術：(1) OptMT — 直接的編譯器最佳化（例如以 -maxrregcount 限制暫存器用量以提升 warp-level parallelism 與佔用率）；(2) RPF — 暫存器駐留的軟體 prefetch，提前發出 load 以掩蓋長延遲 stall；(3) L2P（L2 pinning）— 本文的快取重用核心貢獻：一個專用的 prefetch_pin_L2 kernel 對最熱門的 embedding row 發出 inline PTX 'prefetch.global.L2::evict_last'，將這些 row 以最高保留優先序釘在 GPU L2 cache 中，使其在「跨 kernel 呼叫」之間持續駐留，把原本跨 kernel 的冷 HBM 重讀轉化為 L2 命中。"
  },
  "key_improvements": {
    "en": "Combined OptMT+RPF+L2P improves the embedding stage by up to 103% and the overall DLRM inference pipeline by up to 77% on A100. OptMT (compiler) alone gives up to ~53%. Cache/inter-kernel L2 reuse is central to the motivation (the paper is indexed under 'Cache residency control' and 'Prefetching') but is the smallest incremental knob: L2 pinning contributes only ~13.5% on top of RPF+OptMT, because once register prefetching already hides much of the latency the remaining headroom from pinning hot rows in L2 across kernels is modest.",
    "zh": "在 A100 上，OptMT+RPF+L2P 三者合併可將 embedding 階段提升最高 103%、整體 DLRM 推理管線提升最高 77%。單獨 OptMT（編譯器）可達約 53%。Cache／跨 kernel 的 L2 重用是動機核心（論文索引於『Cache residency control』與『Prefetching』），但屬於增益最小的旋鈕：L2 pinning 相對於 RPF+OptMT 僅再貢獻約 13.5%——因為當暫存器 prefetch 已掩蓋大部分延遲後，再把熱門 row 跨 kernel 釘在 L2 所剩的提升空間有限。"
  },
  "open_source": "Yes (https://github.com/rishucoding/reproduce_MICRO24_GPU_DLRM_inference)",
  "baselines_compared": "Off-the-shelf PyTorch EmbeddingBag (base), OptMT, RPF+OptMT, L2P+OptMT",
  "evaluation_method": "Hardware Testbed",
  "compute_memory_hw": "NVIDIA A100, HBM2e, GPU L2 cache",
  "gpu_count": "A100 GPU",
  "node_count": "Not Specified",
  "modeling_approach": "Measurement only",
  "modeling_granularity": "Kernel",
  "phase_coverage": "Not Specified",
  "reported_accuracy": "Not Reported (this is an optimization/characterization paper, not a latency-prediction model)",
  "workload_family": "Not Specified",
  "models_validated": "DLRM (Deep Learning Recommendation Model)",
  "precision_coverage": "FP16/BF16",
  "parallelism_coverage": "None",
  "batching_policy": "Static",
  "kv_cache_treatment": "Not Specified",
  "validation_workloads": "DLRM embedding access traces with hot/cold row partitioning across memory-access patterns (one-item, high-hot, med-hot, low-hot, random)",
  "tail_latency": "Mean only",
  "downstream_use": "Hardware co-design",
  "scope_assumptions": {
    "en": "Focuses on the embedding (gather) stage of DLRM inference on a single NVIDIA A100; techniques are implemented as drop-in modifications to the PyTorch EmbeddingBag CUDA kernel (built against PyTorch v2.1.0, CUDA arch sm_80). L2 pinning relies on the sm_80 'prefetch.global.L2::evict_last' PTX residency-control primitive and on a static hot-row index list; results are reported for A100-class L2 capacity and do not model multi-GPU, quantization, or KV-cache hierarchies.",
    "zh": "聚焦於單張 NVIDIA A100 上 DLRM 推理的 embedding（gather）階段；各技術以對 PyTorch EmbeddingBag CUDA kernel 的即插即用修改實作（基於 PyTorch v2.1.0、CUDA 架構 sm_80）。L2 pinning 依賴 sm_80 的 'prefetch.global.L2::evict_last' PTX 駐留控制原語與一份靜態的熱門 row 索引清單；結果以 A100 等級的 L2 容量回報，未涵蓋多 GPU、量化或 KV-cache 層級的建模。"
  },
  "source": {
    "extracted_by": "paper-extract (l2_reuse study)",
    "extracted_at": "2026-06-14",
    "repo_cloned": "https://github.com/rishucoding/reproduce_MICRO24_GPU_DLRM_inference",
    "notes": "Affiliations and incremental-gain framing verified against arXiv:2410.22249 PDF page 1 and the cloned repo's opt_designs/l2_pinning/EmbeddingBag_timed.cu (prefetch.global.L2::evict_last in the prefetch_pin_L2 kernel)."
  }
}
